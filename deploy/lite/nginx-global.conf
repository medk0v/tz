map $http_upgrade $tz_lite_connection_upgrade {
    default upgrade;
    '' close;
}
limit_req_zone $binary_remote_addr zone=tz_lite_api_per_ip:10m rate=30r/s;
limit_req_zone $binary_remote_addr zone=tz_lite_login_per_ip:1m rate=6r/m;
limit_req_zone $binary_remote_addr zone=tz_lite_widget_session_per_ip:1m rate=30r/m;
